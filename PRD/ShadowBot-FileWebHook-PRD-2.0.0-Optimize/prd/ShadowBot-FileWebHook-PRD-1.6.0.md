需求1:
移除核心状态-->基本配置-->触发器文件路径这个配置, 固定使用 用户目录/.filewebhook/triggerFiles 目录作为触发器文件存储路径
注意不止是ui层面移除 还要把后端的逻辑也改掉

需求2:  移除核心状态-->http服务状态 这个区块里面的2个按钮 "重启服务" 和 "停止服务"
我们现在不允许用户手动停止和重启http服务了  我们默认打开filewebhook软件 那就是默认启动http服务
如果用户想要停止http服务  那么只能通过关闭filewebhook软件来实现

需求3: 将核心状态里面的基本配置区块  和 身份配置区块 和 python配置区块  这三个区块  全部合并到1个区块里面  叫做 "核心配置"
并且把核心状态页面右下角那个保存设置按钮  移动到这个新的核心配置区块的右下角

